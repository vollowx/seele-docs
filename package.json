{
  "name": "seele-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npx @11ty/eleventy",
    "dev": "npx @11ty/eleventy && vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@types/bun": "latest"
  },
  "dependencies": {
    "@vollowx/seele": "^0.8.1",
    "lightningcss": "^1.30.2",
    "vite": "^7.3.0",
    "vite-plugin-html": "^3.2.2"
  }
}
