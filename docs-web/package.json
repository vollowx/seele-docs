{
  "name": "seele-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build:ssr-entrypoint && bun run build:client && bun run build:eleventy",
    "build:ssr-entrypoint": "node build-ssr.js",
    "build:client": "node build-client.js",
    "build:eleventy": "eleventy",
    "preview": "wds --root-dir _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2",
    "@lit-labs/eleventy-plugin-lit": "^1.0.6",
    "@lit-labs/ssr": "^4.0.0",
    "@lit-labs/ssr-client": "^1.1.8",
    "@types/bun": "latest",
    "@web/dev-server": "^0.4.6",
    "esbuild": "^0.27.2",
    "html-minifier": "^4.0.0",
    "lightningcss": "^1.31.1"
  },
  "dependencies": {
    "@vollowx/seele": "^0.11.1",
    "iconify-icon": "^3.0.2"
  }
}
